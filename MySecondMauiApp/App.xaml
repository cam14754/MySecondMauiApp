<?xml version="1.0" encoding="UTF-8" ?>
<Application
    x:Class="MySecondMauiApp.App"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MySecondMauiApp">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="LabelText">Black</Color>
            <Color x:Key="LabelTextDark">White</Color>

            <Color x:Key="Primary">#1cd3fc</Color>
            <Color x:Key="PrimaryDark">#2eb3d1</Color>
            <Color x:Key="Accent">#6b77ff</Color>

            <Color x:Key="LightBackground">#FAF9F8</Color>
            <Color x:Key="DarkBackground">#383838</Color>

            <Color x:Key="LighterDarkBackground">#4a4a4a</Color>
            <Color x:Key="DarkerLightBackground">#dedede</Color>

            <Style x:Key="BaseLabel" TargetType="Label">
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource LabelText}, Dark={StaticResource LabelTextDark}}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style
                x:Key="SmallLabel"
                BasedOn="{StaticResource BaseLabel}"
                TargetType="Label">
                <Setter Property="FontSize" Value="12" />
            </Style>

            <Style
                x:Key="MediumLabel"
                BasedOn="{StaticResource BaseLabel}"
                TargetType="Label">
                <Setter Property="FontSize" Value="16" />
            </Style>

            <Style
                x:Key="LargeLabel"
                BasedOn="{StaticResource BaseLabel}"
                TargetType="Label">
                <Setter Property="FontSize" Value="20" />
            </Style>

            <Style
                x:Key="LargeLabelBold"
                BasedOn="{StaticResource LargeLabel}"
                TargetType="Label">
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <Style x:Key="ButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource LightBackground}" />
                <Setter Property="TextColor" Value="{StaticResource Primary}" />
                <Setter Property="BorderColor" Value="{StaticResource Primary}" />
                <Setter Property="BorderWidth" Value="2" />
                <Setter Property="HeightRequest" Value="40" />
                <Setter Property="CornerRadius" Value="20" />
            </Style>

            <Style
                x:Key="TopButton"
                BasedOn="{StaticResource ButtonStyle}"
                TargetType="Button">
                <Setter Property="Margin" Value="4" />
                <Setter Property="WidthRequest" Value="70" />
                <Setter Property="HeightRequest" Value="40" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Padding" Value="0" />
            </Style>

            <Style x:Key="CardView" TargetType="Border">
                <Setter Property="Stroke" Value="{StaticResource Primary}" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="StrokeShape" Value="RoundRectangle 10" />
                <Setter Property="Background" Value="{AppThemeBinding Light={StaticResource DarkerLightBackground}, Dark={StaticResource LighterDarkBackground}}" />
            </Style>

            <Style
                x:Key="SelectedCard"
                BasedOn="{StaticResource CardView}"
                TargetType="Border">
                <Setter Property="Stroke" Value="{StaticResource Accent}" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
